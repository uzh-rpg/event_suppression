numpy>=1.24
h5py>=3.6
hdf5plugin>=3.2
opencv-python>=4.5
scipy>=1.10
tqdm>=4.64
PyYAML>=6.0
albumentations>=1.4.18
numba>=0.65
matplotlib>=3.10
pandas>=2.3
imageio>=2.37

# ev-loader is imported from the checked-out ./ev-loader directory at runtime.
# Do not install it with dependencies here; its upstream requirements include
# optional loaders/visualization packages that are not needed for this project.
